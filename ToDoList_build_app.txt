1) send post request with json

curl -X 'POST' \ curl -X 'POST' \
  'http://127.0.0.1:8000/items/' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "name": "string",
  "price": 0
}'

Разобраться как постманом отправлять так же